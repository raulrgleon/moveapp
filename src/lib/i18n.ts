/** Re-export i18n module (legacy import path). */
export {
  DEFAULT_LOCALE,
  LOCALE_STORAGE_KEY,
  SUPPORTED_LOCALES,
  detectDeviceLocale,
  translate,
  getLocaleLabel,
  type Locale,
} from "./i18n/index";
